<%- include('../partials/header') %> 

    <h1>characters</h1>
    <p>Hello <%= username %> </p>
    
    <% for(let index = 0; index < characters.length; index++) {%>
        <img src="<%= characters[index].characterImage %>" alt="" width="200" height="200">
        <p><%= characters[index].characterName %></p>
    <% } %> 

    <%= message %>